<% if flash[:errors] %>
  <div class="animated bounceInDown log-in-warning-container">
    <div class="log-in-warning modal-content bg-danger">
      <ul>
        <% flash[:errors].each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>
